-- Ideally, these keys should be stored in a credential store
-- However, it's convinient to not enter the password for each session (NOT RECOMMENDED)
vim.g["codegpt_openai_api_key"] = "OPENAI_API_KEY"
